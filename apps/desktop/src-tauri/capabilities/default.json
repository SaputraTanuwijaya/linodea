{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main, alert, and timer windows",
  "windows": ["main", "alert", "timer"],
  "permissions": [
    "core:default",
    "notification:default",
    "autostart:default",
    "dialog:default",
    "opener:default"
  ]
}
