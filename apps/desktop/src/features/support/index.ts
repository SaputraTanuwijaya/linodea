/**
 * Public surface for the support feature.
 *
 * Other layers import from here, never from internal files. This feature is
 * static (external links only) — no hook, no state.
 */

export { supportSettingsSection } from "./settingsSection";
